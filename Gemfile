source 'https://rubygems.org'

# Specify your gem's dependencies in msgpack-rpc-over-http.gemspec
gemspec

platforms :ruby do
  group :test do
    gem 'thin'
  end
end

platforms :jruby do
  group :test do
    gem 'mizuno'
  end
end
